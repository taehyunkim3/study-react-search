import { styled } from "styled-components";

const TopBarContainer = styled.div`
  position: relative;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  margin-bottom: 50px;
`;
const TopBar = () => {
  return (
    //
    <TopBarContainer>box</TopBarContainer>
  );
};

export default TopBar;
